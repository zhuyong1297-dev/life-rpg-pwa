# V5 真实日常闭环设计验收

## 对比对象

- 视觉真值：`.private/v5-figma-mobile-core.png`
- 实现截图：`.private/v5-prototype-qa/today-390.png`
- 合并对比：`.private/v5-prototype-qa/comparison-today-390.png`
- 视口：`390 × 844 CSS px`
- 像素密度：源稿按首个 `390 × 844` 画板裁切；实现使用 `deviceScaleFactor: 1`，截图为 `390 × 844 px`，无需缩放归一化。
- 状态：使用独立预览数据库中的合成验收活动；不读取正式数据库或真实个人数据。

## 完整视图对比

合并图左侧为 Figma 选定方案，右侧为浏览器实现。两者保持相同的信息顺序：

1. 日期与今天标题。
2. 等级、赛季和金币状态。
3. 时间锚点与当前触发行动。
4. 今天随时行动。
5. 本周灵活进度。
6. 五项稳定导航。

实现保留了源稿的浅灰画布、白色内容面、森林绿主操作、琥珀金奖励、8px 以内圆角以及紧凑列表密度。

## 交互证据

- 完成反馈：`.private/v5-prototype-qa/feedback-pixel7.png`
- 周进度时长选择：`.private/v5-prototype-qa/weekly-sheet-pixel7.png`
- 创建行动：`.private/v5-prototype-qa/create-pixel7.png`
- 成长页：`.private/v5-prototype-qa/growth-pixel7.png`
- 320px：`.private/v5-prototype-qa/today-320.png`
- 桌面：`.private/v5-prototype-qa/today-desktop.png`

未单独裁切局部对比，因为 390px 合并图中的标题、按钮、状态条和列表文字均可直接辨认；交互截图用于验证源稿对应状态能够真实运行。

## 必查表面

- 字体与排版：使用中文系统无衬线字体栈；标题、正文、标签和数字层级与源稿一致，无负字距、截断或异常换行。
- 间距与布局：移动端保持单列，桌面使用侧栏、720px 主任务区和右侧状态栏；320、390、412、1440px 均无横向溢出。
- 色彩与语义：主操作、成长、奖励和信息色分别使用绿、金、蓝及领域色，未退化为单色界面；文字与背景保持清晰对比。
- 图片质量：使用经验证的透明旅者位图，不使用 CSS 绘图、占位图或手工 SVG；小尺寸仍可识别。
- 文案：功能名称优先，RPG 词汇只作辅助；时间不固定的行为明确进入“今天随时”和“本周灵活”。

## 对比历史

### 第一次

- `[P2]` 今天标题下的重复说明增加了约一行高度，使本周进度在 `390 × 844` 首屏中被底部导航遮住。
- 修复：移除今天页重复说明，保留“时间锚点 + 灵活行动”区域内的解释。
- 修复证据：重新生成 `.private/v5-prototype-qa/today-390.png` 和合并对比图；主行动、随时行动与本周进度的可见顺序恢复。

### 第二次

- 未发现可执行的 P0、P1 或 P2 问题。
- 本周进度卡片在 390px 首屏只露出主要部分，但可以正常滚动到导航上方，操作没有被永久遮挡，归类为可接受的 P3 密度差异。

## 自动化结果

- 320、390、Pixel 7 和 1440px：无横向溢出。
- 浏览器控制台：无错误。
- 核心交互：真实完成事务、反馈、10 秒撤销、周时长选择、成长导航和真实创建均已执行。
- 当前行动常态高度约 `130px`；320px 的基础/标准按钮保持同一行，宽度各 `137px`、高度 `44px`，页面无横向溢出。
- 完成后先保留约 1 秒反馈，再推进下一项；刷新后 XP、金币与完成状态仍由 `earth-online-preview-v2` 恢复。
- 15 项每日行动时首页只显示 5 项，5 项周行动时只显示 3 项；390px 和 320px 的完整列表弹层、搜索、完成摘要与今日优先无横向溢出。
- 独立执行时间优先旧 `cue` 文本，跨午夜时间按 `04:00` 游戏日排序；无时间行动的今日优先跨刷新保留并在新游戏日失效。

## 成长页与交互状态验收

- 成长页 320px：`.private/v5-prototype-qa/growth-320.png`
- 成长页 390px：`.private/v5-prototype-qa/growth-390.png`
- 成长页 1440px：`.private/v5-prototype-qa/growth-1440.png`
- 领域详情 320px：`.private/v5-prototype-qa/domain-detail-320.png`
- 领域详情 390px：`.private/v5-prototype-qa/domain-detail-390.png`
- 领域详情 1440px：`.private/v5-prototype-qa/domain-detail-1440.png`

旅者主卡中的等级、当前等级进度、累计 XP、金币和重点领域按同一信息顺序呈现，原页面底部重复的“总成长”信息行已删除。六个领域在全部视口保持两列三行，并作为真实按钮进入详情。

回归测试直接渲染成长页并检查可见文案：必须包含“累计成长”和“持有金币”，不得出现旧行中的“总成长”或“当前持有”。不再以单个 CSS 类是否存在代替用户可见结果。

桌面领域按钮悬停时，背景由透明变为浅绿色，边框变为强调绿色，箭头向右平移 `2px`；复用设置页的次级按钮和分段控件获得同一反馈。键盘焦点环为 `3px`，关闭弹层后焦点返回原领域按钮；触摸控件保留按下反馈，减少动态效果时不发生位移。

领域弹层在 320px 使用 `85vh` 底部弹层，390px 与桌面均无横向溢出；`Escape`、焦点循环和背景滚动锁定均已验证。未发现可执行的 P0、P1 或 P2 问题。

## 后续微调

- `[P3]` 复盘、愿望和设置暂时复用 V4.5 真实页面，后续按相同设计体系逐页重构。

final result: passed
