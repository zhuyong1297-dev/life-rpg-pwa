# 地球 Online 长期记忆

- 一个功能一个任务；规划结论文件化后用新任务实现，不继承长对话。规格和源码按需检索，常驻上下文受 `pnpm context:check` 约束。
- 手机 IndexedDB 是唯一运行事实来源；正式版与预览版永远隔离，不自动复制、同步或重写历史数据。
- 用户偏好明确功能名、紧凑页面、少量关键行动、人物与进度优先、低频信息按需展开，以及克制的 RPG 氛围。
- “更新版本”“发布版本”“提交正式版”表示完成测试、正式部署、线上核验、标签、Release 和文档同步；明确“仅预览”时例外。
- Git HTTPS 在当前网络经常超时。发布优先 `pnpm publish:api`，按远程 SHA/tree 检查点续跑，必要时用 `gh api`；禁止自动强推。
- PWA 更新前先导出 JSON；不得用卸载应用或清除站点数据解决缓存问题。
- Dexie 旧对象不会自动获得 Zod 默认值；新增兼容字段时，读取和写事务都先规范化，并用真实形态的旧记录测试。
- 新用户先完成一条真实行动：空库不进入旧迁移，七日体验不惩罚、不加奖；推广反馈只由用户主动发送邮件，应用无遥测。
- 推荐只负责预填通用起点；未经用户预览和确认，不得创建活动、替换规划或改变赛季。
- MEMORY 不是 changelog。产品行为、数据契约和发布历史分别以代码/Tech-Spec、PRD 和 Release/Version 为准。
